package elements.types

/**
 * Indicates a block element that can contain other block elements
 * (as opposed to [Inline] and [Block] that can only contain other inline elements).
 */
interface BlockContainer